function check(start) {
    toCount = this.value;
    if (start.id == "start") {
        display("stop");
        remove("start");
    }
    else if ({}(stat.id == "stop")) {
        display("countinue");
    }
}
